
buttons:
    use mechanical buttons?
    test capacitive buttons on a print?
    ? use capacitive 12 channel?

invert pattern display row

pattern display: 
    ? invert current step
    ? set current step to off when clock is high
    ? invert current step when clock is high
    ?

log midi handler: 
    use hard coded string, overwrite bytes, use write not printf
    
idem debug log??

digital out matrix 
    interface set(row, col)  update/write()
    impl: max7219, invert rows/cols if needed, uint8[8] per display, use bitwise

Bitwise ~ template (uint32_t , uint8_t, ...)

